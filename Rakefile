require_relative 'config/environment'
require 'sinatra/activerecord/rake'

require 'rest-client'
test = RestClient.get(http://api.musixmatch.com/ws/1.1/track_id)

desc 'starts a console'
task :console do
  Pry.start
end
